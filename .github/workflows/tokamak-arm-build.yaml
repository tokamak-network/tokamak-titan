name: Tokamak Optimism Publish (Nightly Version)

on: push

jobs:
  start-runners:
    name: Checking EC runners info
    runs-on: ubuntu-latest
    needs:
      [
        start-runners-0,
        start-runners-1,
        start-runners-2,
        start-runners-3,
        start-runners-4,
        start-runners-5,
        start-runners-6,
        start-runners-7,
      ]
    outputs:
      label-0: ${{ needs.start-runners-0.outputs.label }}
      label-1: ${{ needs.start-runners-1.outputs.label }}
      label-2: ${{ needs.start-runners-2.outputs.label }}
      label-3: ${{ needs.start-runners-3.outputs.label }}
      label-4: ${{ needs.start-runners-4.outputs.label }}
      label-5: ${{ needs.start-runners-5.outputs.label }}
      label-6: ${{ needs.start-runners-6.outputs.label }}
      label-7: ${{ needs.start-runners-7.outputs.label }}
      label-8: ${{ needs.start-runners-8.outputs.label }}
      label-9: ${{ needs.start-runners-9.outputs.label }}
      label-10: ${{ needs.start-runners-10.outputs.label }}
      ec2-instance-id-0: ${{ needs.start-runners-0.outputs.ec2-instance-id }}
      ec2-instance-id-1: ${{ needs.start-runners-1.outputs.ec2-instance-id }}
      ec2-instance-id-2: ${{ needs.start-runners-2.outputs.ec2-instance-id }}
      ec2-instance-id-3: ${{ needs.start-runners-3.outputs.ec2-instance-id }}
      ec2-instance-id-4: ${{ needs.start-runners-4.outputs.ec2-instance-id }}
      ec2-instance-id-5: ${{ needs.start-runners-5.outputs.ec2-instance-id }}
      ec2-instance-id-6: ${{ needs.start-runners-6.outputs.ec2-instance-id }}
      ec2-instance-id-7: ${{ needs.start-runners-7.outputs.ec2-instance-id }}
      ec2-instance-id-8: ${{ needs.start-runners-8.outputs.ec2-instance-id }}
      ec2-instance-id-9: ${{ needs.start-runners-9.outputs.ec2-instance-id }}
      ec2-instance-id-10: ${{ needs.start-runners-10.outputs.ec2-instance-id }}
    steps:
      - name: Runners status
        run: |
          echo "EC2 instances id:"
          echo ${{ needs.start-runners-0.outputs.ec2-instance-id }}
          echo ${{ needs.start-runners-1.outputs.ec2-instance-id }}
          echo ${{ needs.start-runners-2.outputs.ec2-instance-id }}
          echo ${{ needs.start-runners-3.outputs.ec2-instance-id }}
          echo ${{ needs.start-runners-4.outputs.ec2-instance-id }}
          echo ${{ needs.start-runners-5.outputs.ec2-instance-id }}
          echo ${{ needs.start-runners-6.outputs.ec2-instance-id }}
          echo ${{ needs.start-runners-7.outputs.ec2-instance-id }}
          echo "Runner labels:"
          echo ${{ needs.start-runners-0.outputs.label }}
          echo ${{ needs.start-runners-1.outputs.label }}
          echo ${{ needs.start-runners-2.outputs.label }}
          echo ${{ needs.start-runners-3.outputs.label }}
          echo ${{ needs.start-runners-4.outputs.label }}
          echo ${{ needs.start-runners-5.outputs.label }}
          echo ${{ needs.start-runners-6.outputs.label }}
          echo ${{ needs.start-runners-7.outputs.label }}
          echo "Finished setup arm64 runners"

  start-runners-0:
    name: Start EC2 Runner 0
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  start-runners-1:
    name: Start EC2 Runner 1
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  start-runners-2:
    name: Start EC2 Runner 2
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  start-runners-3:
    name: Start EC2 Runner 3
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  start-runners-4:
    name: Start EC2 Runner 4
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  start-runners-5:
    name: Start EC2 Runner 5
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  start-runners-6:
    name: Start EC2 Runner 6
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  start-runners-7:
    name: Start EC2 Runner 7
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  start-runners-8:
    name: Start EC2 Runner 8
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  start-runners-9:
    name: Start EC2 Runner 9
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  start-runners-10:
    name: Start EC2 Runner 10
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          ec2-image-id: ami-01bd9dc38789ab007
          ec2-instance-type: t4g.2xlarge
          subnet-id: subnet-013cf87174bb21c25
          security-group-id: sg-03165a21a3ba6e8b3

  l2geth:
    name: Publish L2Geth
    needs: start-runners
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./l2geth/package.json)
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux -f ./l2geth/Dockerfile -t onthertech/titan-l2geth:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-l2geth:${{ steps.runner_info.outputs.architecture }}

  gas-oracle:
    name: Publish Gas Oracle
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./gas-oracle/package.json)
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux -f ./gas-oracle/Dockerfile -t onthertech/titan-gas-oracle:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-gas-oracle:${{ steps.runner_info.outputs.architecture }}

  hardhat-node:
    name: Publish Hardhat Node
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get version from package.json
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./ops/docker/hardhat/package.json)
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux -f ./ops/docker/hardhat/Dockerfile -t onthertech/titan-hardhat:${{ steps.runner_info.outputs.architecture }} ./ops/docker/hardhat
          docker image ls
          docker push onthertech/titan-hardhat:${{ steps.runner_info.outputs.architecture }}

  message-relayer:
    name: Publish Message Relayer
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}
      arch: ${{ runner.architecture }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./packages/message-relayer/package.json)
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)
          echo ${{ runner.architecture }}

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux --target message-relayer -f ./ops/docker/Dockerfile.packages -t onthertech/optimism.message-relayer:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/optimism.message-relayer:${{ steps.runner_info.outputs.architecture }}

  titan-message-relayer:
    name: Publish Titan Message Relayer
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./packages/tokamak/message-relayer/package.json)
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux --target titan-message-relayer -f ./ops/docker/Dockerfile.packages -t onthertech/titan-message-relayer:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-message-relayer:${{ steps.runner_info.outputs.architecture }}

  fault-detector:
    name: Publish Fault Detector
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./packages/fault-detector/package.json)
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux --target fault-detector -f ./ops/docker/Dockerfile.packages -t onthertech/titan-fault-detector:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-fault-detector:${{ steps.runner_info.outputs.architecture }}

  drippie-mon:
    name: Publish Drippie Monitor
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./packages/drippie-mon/package.json)
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux --target drippie-mon -f ./ops/docker/Dockerfile.packages -t onthertech/titan-drippie-mon:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-drippie-mon:${{ steps.runner_info.outputs.architecture }}

  data-transport-layer:
    name: Publish Data Transport Layer
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./packages/data-transport-layer/package.json)
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux --target data-transport-layer -f ./ops/docker/Dockerfile.packages -t onthertech/titan-data-transport-layer:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-data-transport-layer:${{ steps.runner_info.outputs.architecture }}

  contracts:
    name: Publish Deployer
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./packages/tokamak/contracts/package.json)
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux --target titan-deployer -f ./ops/docker/Dockerfile.packages -t onthertech/titan-deployer:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-deployer:${{ steps.runner_info.outputs.architecture }}

  proxyd:
    name: Publish proxyd
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./proxyd/package.json)
          echo ::set-output name=GITDATE::"$(date +%d-%m-%Y)"
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Build and push
        run: |
          docker build --build-arg GITVERSION=${{ steps.extractver.outputs.VERSION }} --build-arg GITCOMMIT=${{ steps.extractver.outputs.GITSHA }} --build-arg GITDATE=${{ steps.extractver.outputs.GITDATE }} --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux -f ./proxyd/Dockerfile -t onthertech/titan-proxyd:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-proxyd:${{ steps.runner_info.outputs.architecture }}

  op-exporter:
    name: Publish op-exporter
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./op-exporter/package.json)
          echo ::set-output name=GITDATE::"$(date +%d-%m-%Y)"
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Build and push
        run: |
          docker build --build-arg GITVERSION=${{ steps.extractver.outputs.VERSION }} --build-arg GITCOMMIT=${{ steps.extractver.outputs.GITSHA }} --build-arg GITDATE=${{ steps.extractver.outputs.GITDATE }} --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux -f ./op-exporter/Dockerfile -t onthertech/titan-op-exporter:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-op-exporter:${{ steps.runner_info.outputs.architecture }}

  l2geth-exporter:
    name: Publish l2geth-exporter
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./l2geth-exporter/package.json)
          echo ::set-output name=GITDATE::"$(date +%d-%m-%Y)"
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Build and push
        run: |
          docker build --build-arg GITVERSION=${{ steps.extractver.outputs.VERSION }} --build-arg GITCOMMIT=${{ steps.extractver.outputs.GITSHA }} --build-arg GITDATE=${{ steps.extractver.outputs.GITDATE }} --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux -f ./l2geth-exporter/Dockerfile -t onthertech/titan-l2geth-exporter:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-l2geth-exporter:${{ steps.runner_info.outputs.architecture }}

  batch-submitter-service:
    name: Publish batch-submitter-service
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./batch-submitter/package.json)
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux -f ./batch-submitter/Dockerfile -t onthertech/titan-batch-submitter-service:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-batch-submitter-service:${{ steps.runner_info.outputs.architecture }}

  indexer:
    name: Publish indexer
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./indexer/package.json)
          echo ::set-output name=GITDATE::"$(date +%d-%m-%Y)"
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Build and push
        run: |
          docker build --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux -f ./indexer/Dockerfile -t onthertech/titan-indexer:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-indexer:${{ steps.runner_info.outputs.architecture }}

  teleportr:
    name: Publish Teleportr
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        runner: [ubuntu-latest, ARM64]
    needs: start-runners
    outputs:
      newVersion: ${{ steps.extractver.outputs.VERSION }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Get version
        id: extractver
        run: |
          echo ::set-output name=VERSION::$(jq -r .version ./teleportr/package.json)
          echo ::set-output name=GITDATE::"$(date +%d-%m-%Y)"
          echo ::set-output name=GITSHA::$(echo ${GITHUB_SHA} | cut -c1-8)

      - name: Check Runner Architecture
        id: runner_info
        run: |
          ARCHITECTURE=$(uname -m)
          if [ "$ARCHITECTURE" = "x86_64" ]; then
            ARCHITECTURE="amd64"
          fi
          if [ "$ARCHITECTURE" = "aarch64" ]; then
            ARCHITECTURE="arm64"
          fi
          echo "::set-output name=architecture::$ARCHITECTURE"

      - name: Build and push
        run: |
          docker build --build-arg GITVERSION=${{ steps.extractver.outputs.VERSION }} --build-arg GITCOMMIT=${{ steps.extractver.outputs.GITSHA }} --build-arg GITDATE=${{ steps.extractver.outputs.GITDATE }} --build-arg TARGETARCH=${{ steps.runner_info.outputs.architecture }} --build-arg TARGETOS=linux -f ./teleportr/Dockerfile -t onthertech/titan-teleportr:${{ steps.runner_info.outputs.architecture }} .
          docker image ls
          docker push onthertech/titan-teleportr:${{ steps.runner_info.outputs.architecture }}

  multi-arch-image:
    name: Build multi-arch image
    needs:
      - l2geth
      - gas-oracle
      - hardhat-node
      - message-relayer
      - titan-message-relayer
      - fault-detector
      - drippie-mon
      - data-transport-layer
      - contracts
      - proxyd
      - op-exporter
      - l2geth-exporter
      - batch-submitter-service
      - indexer
      - teleportr
    runs-on: ubuntu-latest
    steps:
      - name: Setup manifest-tool
        run: |
          wget https://github.com/estesp/manifest-tool/releases/download/v1.0.3/manifest-tool-linux-amd64
          chmod +x manifest-tool-linux-amd64
          sudo mv manifest-tool-linux-amd64 /usr/local/bin/manifest-tool

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_USERNAME }}
          password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN_SECRET }}

      - name: Build and Push Docker Image
        run: |
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-l2geth:ARCH --target onthertech/titan-l2geth:test-${{ needs.l2geth.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-l2geth:ARCH --target onthertech/titan-l2geth:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-gas-oracle:ARCH --target onthertech/titan-gas-oracle:test-${{ needs.gas-oracle.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-gas-oracle:ARCH --target onthertech/titan-gas-oracle:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-hardhat:ARCH --target onthertech/titan-hardhat:test-${{ needs.hardhat-node.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-hardhat:ARCH --target onthertech/titan-hardhat:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/optimism.message-relayer:ARCH --target onthertech/optimism.message-relayer:test-${{ needs.message-relayer.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/optimism.message-relayer:ARCH --target onthertech/optimism.message-relayer:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-message-relayer:ARCH --target onthertech/titan-message-relayer:test-${{ needs.titan-message-relayer.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-message-relayer:ARCH --target onthertech/titan-message-relayer:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-fault-detector:ARCH --target onthertech/titan-fault-detector:test-${{ needs.fault-detector.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-fault-detector:ARCH --target onthertech/titan-fault-detector:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-drippie-mon:ARCH --target onthertech/titan-drippie-mon:test-${{ needs.drippie-mon.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-drippie-mon:ARCH --target onthertech/titan-drippie-mon:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-data-transport-layer:ARCH --target onthertech/titan-data-transport-layer:test-${{ needs.data-transport-layer.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-data-transport-layer:ARCH --target onthertech/titan-data-transport-layer:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-deployer:ARCH --target onthertech/titan-deployer:test-${{ needs.contracts.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-deployer:ARCH --target onthertech/titan-deployer:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-proxyd:ARCH --target onthertech/titan-proxyd:test-${{ needs.proxyd.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-proxyd:ARCH --target onthertech/titan-proxyd:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-op-exporter:ARCH --target onthertech/titan-op-exporter:test-${{ needs.op-exporter.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-op-exporter:ARCH --target onthertech/titan-op-exporter:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-l2geth-exporter:ARCH --target onthertech/titan-l2geth-exportertest-${{ needs.l2geth-exporter.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-l2geth-exporter:ARCH --target onthertech/titan-l2geth-exportertest

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-batch-submitter-service:ARCH --target onthertech/titan-batch-submitter-service:test-${{ needs.batch-submitter-service.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-batch-submitter-service:ARCH --target onthertech/titan-batch-submitter-service:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-indexer:ARCH --target onthertech/titan-indexer:test-${{ needs.indexer.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-indexer:ARCH --target onthertech/titan-indexer:test

          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-teleportr:ARCH --target onthertech/titan-teleportr:test-${{ needs.teleportr.outputs.newVersion}}
          manifest-tool push from-args --platforms linux/amd64,linux/arm64 --template onthertech/titan-teleportr:ARCH --target onthertech/titan-teleportr:test

  stop-runner:
    name: Stop self-hosted EC2 runner
    needs:
      - start-runners
      - l2geth
      - gas-oracle
      - hardhat-node
      - message-relayer
      - titan-message-relayer
      - fault-detector
      - drippie-mon
      - data-transport-layer
      - contracts
      - proxyd
      - op-exporter
      - l2geth-exporter
      - batch-submitter-service
      - indexer
      - teleportr
    runs-on: ubuntu-latest
    if: ${{ always() }} # required to stop the runner even if the error happened in the previous jobs
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Stop EC2 runner 0
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-0 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-0 }}

      - name: Stop EC2 runner 1
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-1 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-1 }}

      - name: Stop EC2 runner 2
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-2 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-2 }}

      - name: Stop EC2 runner 3
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-3 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-3 }}

      - name: Stop EC2 runner 4
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-4 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-4 }}

      - name: Stop EC2 runner 5
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-5 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-5 }}

      - name: Stop EC2 runner 6
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-6 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-6 }}

      - name: Stop EC2 runner 7
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-7 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-7 }}

      - name: Stop EC2 runner 8
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-8 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-8 }}

      - name: Stop EC2 runner 9
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-9 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-9 }}

      - name: Stop EC2 runner 10
        uses: machulav/ec2-github-runner@v2
        with:
          mode: stop
          github-token: ${{ secrets.BOT_ACCESS_TOKEN }}
          label: ${{ needs.start-runners.outputs.label-10 }}
          ec2-instance-id: ${{ needs.start-runners.outputs.ec2-instance-id-10 }}
