{
  "name": "fee-token-sendtx",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf node_modules && rm yarn.lock && rm package-lock.json",
    "use:ton": "FEE_TOKEN=TON node ./index.js",
    "use:eth": "FEE_TOKEN=ETH node ./index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@ethersproject/transactions": "^5.6.2",
    "dotenv": "^16.0.0",
    "ethers": "^5.6.1"
  }
}
